vimrc setting

[for vim env setting]
1. git clone https://github.com/gmarik/Vundle.vim.git ~/.vim/bundle/Vundle.vim
2. donwlaods vimrc
3. PluginInstall

[for cscope setting]
0. mkdir ~/.vim/plugin
1. mv cscope_maps.vim ~/.vim/plugin
